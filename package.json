{
  "name": "finals-scheduler",
  "version": "1.0.0",
  "description": "IT Scheduler application",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "npm --prefix backend/server run dev",
    "frontend": "npm --prefix frontend start"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "jsonwebtoken": "^9.0.2",
    "lucide-react": "^0.556.0"
  }
}
